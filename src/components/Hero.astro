<section id="hero">
  <div class="hero-blobs" aria-hidden="true">
    <div class="blob blob-1"></div>
    <div class="blob blob-2"></div>
    <div class="blob blob-3"></div>
    <div class="blob blob-4"></div>
  </div>

  <div class="hero-content">
    <div class="hero-badge reveal">
      <span class="lang-es">Espacio cultural &amp; comunitario</span>
      <span class="lang-en">Cultural &amp; community space</span>
    </div>

    <h1 class="hero-title reveal reveal-delay-1">SOMA</h1>

    <p class="hero-tagline reveal reveal-delay-2">
      <span class="lang-es">Cuerpo · Corazón · Movimiento · Presencia · Conexión</span>
      <span class="lang-en">Body · Heart · Movement · Presence · Connection</span>
    </p>

    <div class="hero-description reveal reveal-delay-3">
      <p>
        <span class="lang-es">Un espacio donde el arte, la sanación y la comunidad se encuentran.</span>
        <span class="lang-en">A space where art, healing, and community come together.</span>
      </p>
    </div>

    <div class="hero-cta reveal reveal-delay-4">
      <a href="#about" class="btn btn-primary">
        <span class="lang-es">Descubre Soma</span>
        <span class="lang-en">Discover Soma</span>
      </a>
      <a href="#events" class="btn btn-secondary">
        <span class="lang-es">Próximos eventos</span>
        <span class="lang-en">Upcoming events</span>
      </a>
    </div>
  </div>

  <div class="scroll-indicator reveal reveal-delay-4" aria-hidden="true">
    <div class="scroll-line"></div>
  </div>
</section>

<style>
  #hero {
    position: relative;
    min-height: 100vh;
    min-height: 100dvh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 6rem 2rem 4rem;
    overflow: hidden;
  }

  /* ===== Organic Blobs ===== */
  .hero-blobs {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.22;
    will-change: transform;
    animation: blobFloat 20s ease-in-out infinite;
  }

  .blob-1 {
    width: 420px;
    height: 420px;
    background: var(--yellow);
    top: -8%;
    right: -6%;
    animation-delay: 0s;
  }

  .blob-2 {
    width: 360px;
    height: 360px;
    background: var(--green);
    bottom: -12%;
    left: -6%;
    animation-delay: -5s;
  }

  .blob-3 {
    width: 240px;
    height: 240px;
    background: var(--terracotta);
    top: 38%;
    right: 12%;
    animation-delay: -10s;
  }

  .blob-4 {
    width: 180px;
    height: 180px;
    background: var(--green-light);
    top: 18%;
    left: 8%;
    animation-delay: -15s;
  }

  @keyframes blobFloat {
    0%,
    100% {
      transform: translate(0, 0) scale(1);
    }
    25% {
      transform: translate(20px, -30px) scale(1.05);
    }
    50% {
      transform: translate(-15px, 20px) scale(0.95);
    }
    75% {
      transform: translate(25px, 15px) scale(1.02);
    }
  }

  /* ===== Content ===== */
  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 820px;
  }

  .hero-badge {
    display: inline-block;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--green);
    border: 1.5px solid var(--green);
    padding: 0.5rem 1.3rem;
    border-radius: var(--radius-full);
    margin-bottom: 2.5rem;
  }

  .hero-title {
    font-family: var(--font-serif);
    font-size: clamp(5rem, 18vw, 13rem);
    color: var(--green-dark);
    letter-spacing: 0.06em;
    line-height: 0.85;
    margin-bottom: 1.5rem;
  }

  .hero-tagline {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: clamp(1.05rem, 2.5vw, 1.45rem);
    color: var(--text-light);
    margin-bottom: 1.5rem;
    letter-spacing: 0.02em;
  }

  .hero-description p {
    font-size: 1.15rem;
    color: var(--text-light);
    max-width: 480px;
    margin: 0 auto 2.5rem;
    line-height: 1.7;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ===== Scroll Indicator ===== */
  .scroll-indicator {
    position: absolute;
    bottom: 2.5rem;
    left: 50%;
    transform: translateX(-50%);
  }

  .scroll-line {
    width: 1px;
    height: 44px;
    background: linear-gradient(to bottom, transparent, var(--text-light));
    opacity: 0.35;
    animation: scrollPulse 2.5s ease-in-out infinite;
  }

  @keyframes scrollPulse {
    0% {
      opacity: 0;
      transform: scaleY(0);
      transform-origin: top;
    }
    30% {
      opacity: 0.35;
      transform: scaleY(1);
      transform-origin: top;
    }
    60% {
      opacity: 0.35;
      transform: scaleY(1);
      transform-origin: bottom;
    }
    100% {
      opacity: 0;
      transform: scaleY(0);
      transform-origin: bottom;
    }
  }

  @media (max-width: 768px) {
    #hero {
      padding: 7rem 1.25rem 3rem;
    }

    .blob-1 {
      width: 260px;
      height: 260px;
    }
    .blob-2 {
      width: 220px;
      height: 220px;
    }
    .blob-3 {
      width: 160px;
      height: 160px;
    }
    .blob-4 {
      width: 120px;
      height: 120px;
    }

    .hero-badge {
      margin-bottom: 2rem;
    }

    .hero-cta {
      flex-direction: column;
      align-items: center;
    }

    .scroll-indicator {
      bottom: 1.5rem;
    }
  }
</style>

