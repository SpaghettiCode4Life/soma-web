---
const navLinks = [
  { href: '#about', label: 'Soma' },
  { href: '#gallery', label: 'Galería' },
  { href: '#events', label: 'Agenda' },
  { href: '#booking', label: 'Espacio' },
];
---

<footer class="footer">
  <div class="container">
    <div class="footer__top">
      <div class="footer__brand">
        <a href="#" class="footer__logo" aria-label="Soma">
          <svg class="footer__logo-icon" viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
            <rect x="1.5" y="1.5" width="61" height="61" rx="12" fill="#FFFBF4" stroke="#1A1A2E" stroke-width="3"/>
            <text x="32" y="44" text-anchor="middle" font-family="'Syne', Arial Black, sans-serif" font-weight="900" font-size="30" fill="#FF5C38" letter-spacing="1">S</text>
            <circle cx="50" cy="14" r="5" fill="#BFFF00" stroke="#1A1A2E" stroke-width="1.5"/>
          </svg>
        </a>
        <p class="footer__tagline">
          Espacio creativo compartido.<br/>
          Cuerpo, arte y encuentro.
        </p>
      </div>

      <nav class="footer__nav">
        <h4 class="footer__nav-title">Navegar</h4>
        <ul>
          {navLinks.map((link) => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </nav>

      <div class="footer__contact">
        <h4 class="footer__nav-title">Contacto</h4>
        <ul>
          <li><a href="mailto:hola@soma.space">hola@soma.space</a></li>
          <li><a href="https://instagram.com/soma.space" target="_blank" rel="noopener">@soma.space</a></li>
        </ul>
      </div>

      <div class="footer__newsletter">
        <h4 class="footer__nav-title">Mantente cerca</h4>
        <p class="footer__newsletter-text">
          Recibe la agenda y novedades.
        </p>
        <form class="footer__form" onsubmit="return false;">
          <input
            type="email"
            placeholder="tu@email.com"
            class="footer__input"
            aria-label="Email"
          />
          <button type="submit" class="footer__submit" aria-label="Suscribir">→</button>
        </form>
      </div>
    </div>

    <div class="footer__bottom">
      <p class="footer__copy">© 2026 Soma. Con cariño y en comunidad.</p>
      <div class="footer__deco" aria-hidden="true">
        <span>✦</span>
        <span>✦</span>
        <span>✦</span>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--ink);
    color: var(--cream);
    padding: 5rem 0 2rem;
    border-top: var(--border);
  }

  .footer__top {
    display: grid;
    grid-template-columns: 1.5fr 1fr 1fr 1.3fr;
    gap: 3rem;
    margin-bottom: 4rem;
  }

  /* Logo — favicon SVG icon */
  .footer__logo {
    display: inline-flex;
    align-items: center;
    margin-bottom: 1rem;
    transition: transform 0.2s ease, opacity 0.2s;
  }

  .footer__logo:hover {
    transform: scale(1.05);
  }

  .footer__logo-icon {
    width: 36px;
    height: 36px;
    display: block;
  }

  /* Override fills for dark footer background */
  .footer__logo-icon rect {
    fill: var(--ink);
    stroke: var(--cream);
  }

  .footer__logo-icon text {
    fill: var(--coral);
  }

  .footer__logo-icon circle {
    fill: var(--lime);
    stroke: var(--cream);
  }

  .footer__tagline {
    opacity: 0.5;
    font-size: 0.9rem;
    line-height: 1.6;
  }

  .footer__nav-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 1.2rem;
    color: var(--coral);
  }

  .footer__nav ul,
  .footer__contact ul {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .footer__nav a,
  .footer__contact a {
    font-size: 0.9rem;
    opacity: 0.6;
    transition: opacity 0.2s, color 0.2s;
  }

  .footer__nav a:hover,
  .footer__contact a:hover {
    opacity: 1;
    color: var(--coral);
  }

  .footer__newsletter-text {
    font-size: 0.85rem;
    opacity: 0.5;
    margin-bottom: 1rem;
  }

  .footer__form {
    display: flex;
    border: 2px solid rgba(255, 251, 244, 0.25);
    border-radius: var(--radius-pill);
    overflow: hidden;
    transition: border-color 0.2s;
  }

  .footer__form:focus-within {
    border-color: var(--coral);
  }

  .footer__input {
    flex: 1;
    background: none;
    border: none;
    color: var(--cream);
    padding: 0.65rem 1rem;
    font-family: var(--font-body);
    font-size: 0.85rem;
    outline: none;
    min-width: 0;
  }

  .footer__input::placeholder {
    color: rgba(255, 251, 244, 0.35);
  }

  .footer__submit {
    background: var(--coral);
    border: none;
    color: var(--cream);
    padding: 0.65rem 0.9rem;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1rem;
    cursor: pointer;
    transition: background 0.2s;
  }

  .footer__submit:hover {
    background: var(--pink);
  }

  .footer__bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 251, 244, 0.08);
  }

  .footer__copy {
    font-size: 0.75rem;
    opacity: 0.35;
  }

  .footer__deco {
    display: flex;
    gap: 0.5rem;
    opacity: 0.25;
    font-size: 0.65rem;
  }

  @media (max-width: 768px) {
    .footer__top {
      grid-template-columns: 1fr 1fr;
      gap: 2.5rem;
    }

    .footer__brand {
      grid-column: span 2;
    }

    .footer__newsletter {
      grid-column: span 2;
    }
  }

  @media (max-width: 480px) {
    .footer__top {
      grid-template-columns: 1fr;
    }

    .footer__brand,
    .footer__newsletter {
      grid-column: span 1;
    }

    .footer__bottom {
      flex-direction: column;
      gap: 1rem;
      text-align: center;
    }
  }
</style>
